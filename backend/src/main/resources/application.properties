spring.application.name=backend

spring.datasource.url=${DB_URL:jdbc:postgresql://localhost:5432/meteoinsight}
spring.datasource.username=${DB_USERNAME:meteo}
spring.datasource.password=${DB_PASSWORD:meteo123}

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true